<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- Kept identical to oms-main's parent version so both apps move
		     through Spring Boot upgrades together — a gateway that silently
		     drifts to a different Boot/Cloud generation than the services it
		     fronts is the kind of thing that only shows up as a hard-to-debug
		     compatibility issue months later. -->
		<version>4.1.0</version>
		<relativePath/>
	</parent>

	<groupId>com.giri</groupId>
	<artifactId>oms-gateway</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>oms-gateway</name>
	<description>API gateway (edge auth, rate limiting, routing) in front of the OMS domain services</description>

	<properties>
		<java.version>21</java.version>
		<!-- Same train as oms-main's spring-cloud.version — see the note there. -->
		<spring-cloud.version>2025.1.2</spring-cloud.version>
	</properties>

	<dependencies>
		<!-- Reactive gateway core. Pulls in spring-boot-starter-webflux
		     transitively — do NOT also add spring-boot-starter-web, the two
		     server stacks (Tomcat/servlet vs Netty/reactive) can't coexist
		     cleanly in one app, and Spring Cloud Gateway only runs on the
		     reactive one.
		     NOTE: as of the Spring Cloud Gateway 4.2+ line (pulled in by
		     spring-cloud-dependencies 2025.1.2 below), the old
		     spring-cloud-starter-gateway artifact was split by server type
		     into spring-cloud-starter-gateway-server-webflux and
		     ...-server-webmvc — the BOM no longer manages the old artifact
		     name at all, so pinning that name here left Maven with no
		     version to resolve. This project wants the reactive one. -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
		</dependency>

		<!-- Validates incoming JWTs against oms-main's published JWKS
		     (/.well-known/jwks.json) — reuses the RS256 keypair oms-main
		     already signs with, no shared secret, no new key management. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>

		<!-- Reactive Redis client, backing Spring Cloud Gateway's built-in
		     RequestRateLimiter (Redis token-bucket implementation) — reuses
		     the same Redis instance oms-main already runs, no new infra. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis-reactive</artifactId>
		</dependency>

		<!-- Same Vault instance/KV backend oms-main reads from (see
		     spring.cloud.vault.kv.default-context=oms in application.properties) —
		     supplies REDIS_PASSWORD from secret/oms/<profile>, no shared
		     secret duplicated between the two apps. -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-vault-config</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.projectreactor</groupId>
			<artifactId>reactor-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>

</project>
